import 'package:flutter/material.dart';


class StartUpViewTabPortrait extends StatelessWidget {
  const StartUpViewTabPortrait({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
class StartUpViewTabLandscape extends StatelessWidget {
  const StartUpViewTabLandscape({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
